[build]
builder = "NIXPACKS"
buildCommand = "composer install --no-dev --prefer-dist --optimize-autoloader && php artisan config:clear && php artisan route:clear && php artisan view:clear"

[deploy]
startCommand = "bash scripts/railway-start.sh"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

